typ str : string;
var myString : str;
var secondString : str;

fun setString1( examp : string) : str = (
    { myString = examp },
    { secondString = setString2('this is an example string')}
) { where fun setString2( passed : string): str = (
        { passed = 'example' },
        { thirdString = passed }
    ) { where var thirdString : str}
}