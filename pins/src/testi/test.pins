var x:integer;

fun main(i : integer) : integer = 
(putString('Hello ?'),getString(str), putString(str),putString('Vnesite stevilo: '), getInt(a), {a = foo(a)}, putInt(a),0)
{where var a:integer; var str:string};

fun foo(n:integer):integer = 
({if (n < 5) then {x = n+1} else {x = g(n) + h(n-2)}}, x)
{where var x:integer;
fun g(n:integer):integer = ({for i=0,n,1: {y = i}}, putInt(y), y){where var i:integer; var y:integer};
fun h(n:integer):integer = ({n=k(n)}, putInt(n), n)
};

fun k(n:integer):integer = ({i = 0},{while (i<n): ({i = i+1})}, {x = {x=0} + i}) 
{where var i:integer}
